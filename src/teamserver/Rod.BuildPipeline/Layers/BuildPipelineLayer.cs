namespace Rod.BuildPipeline.Layers;

/// <summary>
/// Marker class for the Payload Build Pipeline layer (architecture.md Sec 4.1).
/// Exists so the architecture tests have a type to anchor dependency-rule
/// checks to.
/// </summary>
public sealed class BuildPipelineLayer
{
}
