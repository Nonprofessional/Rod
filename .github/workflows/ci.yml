name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        # Version comes from global.json (pinned to 10.0.110).
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: src/Rod.TeamServer/Client/package-lock.json
      - name: Build operator UI
        # Emits the React bundle into src/Rod.TeamServer/wwwroot so the .NET
        # build packages it. The output is gitignored.
        working-directory: src/Rod.TeamServer/Client
        run: |
          npm ci
          npm run build
      - name: Restore
        run: dotnet restore Rod.slnx
      - name: Build
        run: dotnet build Rod.slnx --configuration Release --no-restore

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: src/Rod.TeamServer/Client/package-lock.json
      - name: Build operator UI
        working-directory: src/Rod.TeamServer/Client
        run: |
          npm ci
          npm run build
      - name: Restore
        run: dotnet restore Rod.slnx
      - name: Build
        run: dotnet build Rod.slnx --configuration Release --no-restore
      - name: Test
        run: dotnet test Rod.slnx --configuration Release --no-build

  format:
    name: Format check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
      - name: Verify formatting
        run: dotnet format Rod.slnx --verify-no-changes

  dotnet-implant:
    name: .NET implant
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        # The reference .NET implant targets net10.0; the latest SDK satisfies it.
        uses: actions/setup-dotnet@v4
      - name: Restore
        run: dotnet restore Rod.slnx
      - name: Build
        # Building the solution also compiles the implant's gRPC client bindings
        # from the canonical rod.proto via Grpc.Tools (no committed generated
        # code), so a clean build here proves the proto compile path.
        run: dotnet build Rod.slnx --configuration Release --no-restore
      - name: .NET implant unit tests
        # The per-handler unit tests live in a separate xUnit project
        # (implant-dotnet-tests) that is intentionally external to Rod.slnx,
        # mirroring the implant itself. It exercises the internal handlers
        # (collect/exfil/recon/persist/lateral) and runner dispatch via
        # InternalsVisibleTo, without standing up a real teamserver.
        run: dotnet test implant-dotnet-tests/Rod.Implant.Tests.csproj --configuration Release
      - name: .NET implant end-to-end (roadmap M3.3)
        # DotNetImplantTests publishes the reference .NET implant to a temp dir
        # via `dotnet publish`, then runs `dotnet Rod.Implant.dll` as a real
        # subprocess against a real teamserver -- enrolls over HTTP, beacons over
        # mTLS, runs a dispatched shell.exec task. This is the job that proves the
        # .NET implant checks in and tasks end-to-end; it is otherwise skipped
        # when dotnet is not on PATH.
        run: dotnet test Rod.slnx --configuration Release --no-build --filter "FullyQualifiedName~DotNetImplantTests"

  secrets:
    name: Secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Scan full history with gitleaks
        # Run the MIT-licensed gitleaks CLI directly rather than the
        # gitleaks-action wrapper, which requires a paid license key since v2.
        # Same scan the action ran: every commit in history, fail on any finding.
        # Pin the version explicitly so a upstream release cannot silently change
        # results.
        env:
          GITLEAKS_VERSION: "8.27.1"
        run: |
          curl -sSL "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" -o gitleaks.tar.gz
          tar -xzf gitleaks.tar.gz gitleaks
          ./gitleaks detect --source . --verbose
